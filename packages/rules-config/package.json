{
  "name": "@etk/rules-config",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned, hashable ruleset schema for a Merit Open. Everything counsel may change is configuration here, not code.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "zod": "^4.0.0" },
  "devDependencies": { "typescript": "^5.9.0", "vitest": "^5.0.0" }
}
